[project]
name = "langgraph-work"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ahmxdd-shippedit", email = "ahmadbilal2530@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langgraph",
    "langchain",
    "langchain_openai",
    "dotenv",
]

[project.scripts]
langgraph = "langgraph:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
